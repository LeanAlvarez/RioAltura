# 008 — UI v3: layout fluido, temas, FAQ y simulación hasta 20 m

- **Estado:** lista
- **Rama:** feat/008-ui-v3
- **Depende de:** 006 (mapa y capas), 007 (UI v2) — ambas mergeadas
- **Puede ir en paralelo con:** nada (toca el layout completo y regenera las capas)

## Objetivo

Que la app use toda la pantalla en escritorio sin huecos ni desperdicio, se lea bien en cualquier
tamaño y tema, explique de dónde salen sus datos, y pueda simular crecidas hasta 20 m.

## Diagnóstico previo

Problemas observados sobre `main` (`00981f9`), en capturas a 1920 px:

1. **Todo el contenido en una columna centrada**, con mucho espacio vacío a los costados.
2. **La grilla deja huecos**: "Hoy" es más baja que "Próximos días"; "Aguas arriba" queda sola con un
   hueco enorme debajo; "¿Cuánto acierta?" queda sola en su fila.
3. **Los ejes de los gráficos casi no se leen**: tamaño y contraste bajos.
4. **Los chips de escenarios del mapa se cortan abajo.**
5. **No hay regla hidrométrica vertical.**

> **Nota sobre el punto 5.** La regla hidrométrica **no estaba** en la spec 007: ni "regla" ni
> "hidrométrica" aparecen en ese documento. Lo más parecido fue una sugerencia de la revisión de
> diseño ("una mini-regla horizontal con los tres umbrales y un punto en la altura de hoy"), que se
> resolvió por la otra vía que la misma revisión proponía: la línea "faltan X m para…" más la
> tarjeta "¿Qué significa cada nivel?". Acá entra como requisito nuevo, no como algo pendiente.

## Alcance

### L — Layout fluido

- **L1** — Ancho completo con márgenes laterales de **máximo 24 px**. Si se usa un tope de ancho,
  tiene que ser **≥ 1800 px**.
- **L2 — Grilla explícita por breakpoint**, declarada con `grid-template-areas`, no librada al flujo.

  **≥ 1280 px**
  | Fila | Contenido |
  |---|---|
  | 1 | Hoy + regla hidrométrica │ Próximos días │ Qué significa cada nivel — 3 columnas, **misma altura** |
  | 2 | Mapa a ancho completo (**70vh**), con panel lateral derecho: slider, escenarios y curva de superficie afectada |
  | 3 | Evolución y pronóstico (2/3) │ Aguas arriba + Contexto apilados (1/3) |
  | 4 | Historial completo (2/3) │ ¿Cuánto acierta? (1/3) |
  | 5 | FAQ |
  | 6 | Detalle técnico (plegado) |

  **768–1279 px**: 2 columnas con la misma lógica de agrupación.

  **< 768 px**: 1 columna, en este orden exacto: Hoy, Próximos días, Mapa, Evolución, Niveles,
  Aguas arriba, Contexto, Historial, Acierto, FAQ, Detalle técnico.

- **L3 — Sin huecos.** Ninguna celda de la grilla queda vacía en ningún breakpoint. Las tarjetas de
  una misma fila comparten altura.
- **L4 — Ejes de gráficos**: mínimo **13 px** y contraste **AA** en ambos temas.
- **L5 — Regla hidrométrica vertical** junto a la tarjeta "Hoy": escala con los tres umbrales
  marcados y posicionados a escala, más la altura de hoy. Con texto además del color (§7).

### M — Mobile de verdad

- **M1** — A **360, 390 y 768 px**: sin scroll horizontal, sin textos cortados, blancos de toque de
  **44 px** como mínimo.
- **M2** — El mapa ocupa **60vh** y el panel de controles va **debajo**, sin taparlo.
- **M3** — Chips de escenarios en scroll horizontal **con indicador visual**, sin cortes (hoy se
  cortan abajo).
- **M4** — Gráficos legibles a 360 px: menos marcas en el eje X y tooltip al tocar.

### S — Simulación hasta 20 m

- **S1 — Regenerar las capas** hasta 20,00 m: cada **0,25** hasta 10,50; cada **0,5** hasta 13;
  cada **1,0** hasta 20.
- **S2 — Presupuesto ≤ 25 MB comprimido (gzip), que es lo que se transfiere.** Se reporta también el peso en disco. Si se pasa, simplificar más y **documentar** qué se cambió y cuánto
  detalle se perdió.
- **S3 — BBOX**: ampliarlo lo necesario para que el agua nueva no toque los bordes este/oeste hasta
  20 m. Registrar `toca_borde` por capa en `index.json`.
- **S4 — Etiqueta de escenario hipotético.** Por encima de **10,00 m**: "Escenario hipotético: nunca
  registrado en Colón". El aviso de "por encima del rango modelado" pasa de 13 a **20 m**.
- **S5 — Curva de superficie afectada y slider** llegan hasta 20 m.

### T — Tema claro y oscuro

- **T1 — Toggle visible en el header** (sol/luna).
- **T2** — Por defecto sigue `prefers-color-scheme`; la elección del usuario se guarda en
  `localStorage`, **con `try/catch`** (puede fallar en ventana privada o con cookies bloqueadas).
- **T3** — Todos los colores como tokens CSS. Gráficos, leyendas, capas del mapa y controles se ven
  bien en ambos temas.
- **T4** — Capturas de ambos temas en esta spec.

### F — Footer

- Centrado. Arriba el disclaimer; debajo las atribuciones; al final:
  `Desarrollado por Mirai Software en y para la ciudad de Colón, Entre Ríos.`
  con `https://miraisoftware.net` (`target="_blank"`, `rel="noopener"`) sobre "Mirai Software".

### Q — FAQ

Acordeón, **antes** del detalle técnico, con ancla `#faq` y link en el header.

**Los datos salen del código y de la config, no se inventan.** Si una respuesta necesita un dato que
no está en el código ni en CLAUDE.md, se deja marcada como **PENDIENTE** y se avisa.

Preguntas mínimas:

1. ¿De dónde salen los datos? (INA, Prefectura, Google Flood Forecasting, Copernicus DEM)
2. ¿Cada cuánto se actualizan? (alturas cada 1 h; pronóstico cada 6 h; el INA puede publicar con
   demora; Google emite aproximadamente una vez por día)
3. ¿Qué mide la altura del puerto? (hidrómetro de Prefectura; cero local a −0,26 m IGN)
4. ¿Cómo se calcula el pronóstico en metros? (caudal de Google → curva de calibración → ajuste con
   la medición de hoy → rango ±1 m)
5. ¿Qué tan preciso es? (error medio ~0,5 m a 3 días; por qué puede fallar: descargas de Salto
   Grande)
6. ¿Qué significan los niveles? (6,80 / 7,10 / 7,90 m, con los textos ya aprobados en la 007)
7. ¿Cómo se hace el mapa de zonas inundables? (elevación satelital de 30 m, agua conectada al río;
   limitaciones: incluye techos y árboles, no modela defensas, lluvia local ni desagües)
8. ¿Por qué el mapa llega hasta 20 m si nunca pasó? (escenario hipotético)
9. ¿Es un aviso oficial? (No. Links a Prefectura, CARU e INA)
10. ¿Guarda datos míos? (sin cookies de seguimiento ni analytics; sólo la preferencia de tema en tu
    navegador)
11. ¿Quién lo hizo y cómo reporto un error? (Mirai Software, link a la web)

### T3bis — Paleta reactiva de gráficos (seguimiento)

El L4/T3 original (`--fg`/`--warn`/`--error` leídos una sola vez al construir el gráfico) resultó
insuficiente en uso real: no había reactividad al tema (nunca se redibujaba al cambiar), y los
tokens eran genéricos (pensados para texto de UI, no para líneas sobre el fondo de una tarjeta), lo
que dejaba "Historial completo" y "¿Cuánto acierta?" casi ilegibles en tema claro y los tres
umbrales de "Evolución" indistinguibles entre sí. Este seguimiento lo corrige:

- **Fuente única de la paleta**: `frontend/src/graficos/paleta.ts` — colores por rol y por tema
  (claro/oscuro), anchos/patrones de guiones (iguales en los dos temas), el cálculo de contraste
  WCAG y la generación de las custom properties CSS (`--graf-*`) que consumen los cuatro gráficos.
  Ese mismo módulo alimenta el test de contraste (`paleta.test.ts`): no se puede cambiar un color
  sin que el test lo vea.
- **Reactividad**: `theme.ts` expone `onTemaCambia(callback): () => void`. Los cuatro gráficos
  (`grafico.ts`, `historial.ts`, `precision.ts`, `aguasArriba.ts`) se suscriben al montarse, releen
  la paleta y redibujan (sin volver a pedir datos a la API) tanto por el toggle como por un cambio
  de `prefers-color-scheme` del sistema con la página abierta (si no hay tema explícito guardado).
  Cada `mount*` ahora devuelve `{ destroy() }`, que desuscribe y destruye la instancia de uPlot.
- **Leyendas**: la muestra de cada ítem de leyenda usa el mismo color, ancho y patrón de guiones que
  su línea (`fondoTrazoCss`, un `repeating-linear-gradient` armado a partir del mismo array `dash`
  que usa uPlot), no una aproximación con clases CSS sueltas.
- **Tarjeta "Aguas arriba"**: mini gráfico nuevo (Colón vs. aguas arriba, mismos días), con
  `buildAguasArribaChartSeries` (función pura, testeada) alineando ambas series por fecha.

### T3ter — Defectos de la revisión de diseño (medidos en navegador)

Dos defectos graves y varios menores, verificados por el usuario midiendo en el navegador (no
impresiones), sobre `T3bis` ya mergeado:

- **G1 (el más grave) — Etiquetas de la regla hidrométrica corridas de su marca.** La lógica
  anti-colisión de `domain/reglaHidrometrica.ts` separaba etiquetas pisadas sin reservar margen
  contra el borde de la barra, así que la más alta podía terminar flotando afuera. Arreglo elegido:
  **(a) línea guía**, no (b) "solo el número + leyenda aparte". Justificación: la marca de "hoy"
  puede caer en cualquier punto de la escala (varía a diario), así que la separación de etiquetas es
  necesaria siempre, no solo un caso raro; una línea guía resuelve la ambigüedad para *cualquier*
  combinación de valores, mientras que "solo el número" no garantiza espacio suficiente en el peor
  caso (p. ej. "hoy" exactamente sobre un umbral) sin volver a necesitar desplazamiento — y ahí la
  ambigüedad reaparece igual. Con línea guía se mantiene además el nombre completo junto al número
  (mejor para una persona mayor que una leyenda aparte que hay que ir a buscar). Algoritmo nuevo de
  dos pasadas (abajo→arriba, después arriba→abajo) con margen de borde del 15 %: ninguna etiqueta
  queda fuera de `[0, 100]` de la barra, para cualquier altura de hoy (test parametrizado en
  `reglaHidrometrica.test.ts`); cuando el texto se separa de su tick, se dibuja una línea (SVG,
  `.regla-marca-guia`).
- **G2 — Panel del mapa cerrado en mobile.** `map.ts` abría `<details class="capas-panel">` solo a
  `min-width: 900px`. Ahora abre siempre (`detalles.open = true`), sigue plegable, y el `summary`
  pasó de "Altura y zonas inundables" a "Ver el mapa a otra altura" + chevron (mismo arreglo que G4).
- **G3 — "4,25 m" vs. "4,29 m" sin explicar.** Nueva función pura `describeMostrando` (en `capas.ts`,
  no en `map.ts`, para que sea testeable: `map.ts` importa Leaflet y no corre en el entorno Node de
  los tests — ver `capas.test.ts`). Cuando la capa mostrada está redondeada al paso de 0,25 m,
  explica "El mapa muestra la altura más parecida que tenemos (X). Hoy el río está en Y."; si no hubo
  redondeo, mantiene el texto de escenario original.
- **G4 — Sin señal de que FAQ/detalle técnico/panel del mapa se abren.** `display: flex` en los tres
  `<summary>` ya sacaba el triángulo nativo del navegador. Se agregó un chevron `▾` (`::after`) que
  rota al abrir, igual en los tres.
- **G5 — Curva de superficie sin marcar el máximo observado.** `crecida_maxima_observada_m` es un
  campo **opcional** nuevo de `CapaIndex` (lo agrega el agente de `geoprocessing/` en paralelo; ya
  está en el `index.json` real de este worktree). Si está presente y cae dentro del rango simulado,
  `superficieAfectada.ts` dibuja una línea vertical + "máximo observado" en la curva, con
  `xParaAltura` (función pura, testeada) para ubicarla. Si no está, no se dibuja nada — nunca se
  hardcodea el valor (CLAUDE.md §5).
- **Blancos grandes.** "Superficie afectada" comparte fila con el mapa (70vh) a ≥ 1280 px y
  `align-items: stretch` la estiraba sin contenido de sobra. `#tarjeta-superficie` pasó a
  `display:flex; flex-direction:column`, con `.superficie-svg{flex:1}` y `preserveAspectRatio="none"`
  (la curva ya escala X/Y de forma independiente y arbitraria, así que estirarla más no distorsiona
  nada que antes fuera "real"): medido, el hueco bajó de ~320/~270 px (1280/1920) a ~208/~174 px, y lo
  que queda es texto/slider, no vacío. FAQ pasó a 2 columnas a ≥ 1280 px. **No se tocaron** los
  blancos de "Hoy", "¿Qué significa cada nivel?" ni "¿Cuánto acierta?": son consecuencia directa de
  L3 ("las tarjetas de una fila comparten altura"), que sigue siendo un requisito explícito de esta
  spec — achicarlos ahí requeriría romper esa igualación de alturas, y la tarea pedía resolver "al
  menos" superficie y FAQ.
- **Menores:** rótulo de umbral con el número primero ("6,80 m — Alerta", no "Alerta (6,80 m)") en la
  regla y en el gráfico, para que si algo se trunca sea el nombre y no el dato; fondo opaco (no
  traslúcido) en las etiquetas de umbral del gráfico para que no "pisen" la línea de altura real;
  `padding` derecho de los cuatro gráficos uPlot (`grafico.ts`/`historial.ts`/`precision.ts`/
  `aguasArriba.ts`) para que el último rótulo del eje X no se corte contra el borde; flecha "›" sobre
  el degradé de los chips de escenario (el degradé solo era invisible contra chips del mismo
  `--card-bg`), que desaparece al llegar al final del scroll; `min-width` en los chips para que uno
  corto ("Hoy") no se vea como un punto; reordenada "Próximos días" para que la frase sobre el futuro
  vaya antes que "Hoy no hay alerta."; jerga reemplazada (`SUPERFICIE AFECTADA`, su subtítulo,
  leyenda de "Historial completo", "Pronóstico máx.", "Estable en las últimas 24 h", `DETALLE
  TÉCNICO`) por el texto llano pedido.
- **No tocado, fuera de alcance de este worktree:** el texto `"Crecida de referencia"` (jerga table,
  fila "7,66 m") es un `etiqueta` que llega de la API (`/estadisticas` o similar, `api/types.ts`), no
  un string de este frontend — cambiarlo es un dato del backend/seed, no de `ui-v3`. Tampoco se
  agregó una referencia comprensible para "26.094 ha": no hay ningún dato de superficie de la ciudad
  o del partido en el código ni en `dominio.py` del que derivarla sin inventar un número.

## Fuera de alcance

- No se cambian umbrales, curva caudal→altura ni cero del hidrómetro (§5, §9).
- No se cambia el cálculo del nivel de aviso.
- No se agregan trackers, analytics ni servicios pagos (§9). El único almacenamiento local es la
  preferencia de tema.
- No se corrige la latencia del dato del INA: es del worker, no de la UI.
- No se habilita la tarjeta "Qué hacer" (sigue tras `QUE_HACER_HABILITADO = false` hasta que la
  Municipalidad confirme textos y teléfonos).

## Diseño / decisiones

### La crecida máxima observada son 10 m, no 9,06 m

`CRECIDA_MAXIMA_OBSERVADA_M = 10.00` en `backend/app/config/dominio.py`, con esta fuente:

> Informe INA-CARU 2019, "Relevamiento topográfico" (Informe 2),
> https://www.ina.gob.ar/delta/pdf/INA-CARU_2019_Informe2_RelevamientoTopografico.pdf
> En Colón hay una marca MOP de "Crecida Máxima Observada" a 10 m sobre el cero de la regla, con
> cota GPS de 9,752 m IGN. Es el mismo informe del que sale el cero del hidrómetro (−0,26 m IGN).

**Consecuencia en la UI, obligatoria:** el escenario de 9,06 m pasa a llamarse
**"máximo desde 2023 (INA)"**, porque es el máximo de *nuestra serie*, no el histórico, y con el
nombre viejo se lee como si lo fuera. Se agrega el escenario
**"10 m — crecida máxima observada (marca MOP)"**.

Por eso el umbral de "escenario hipotético" son 10 m y no 9,06: entre 9,06 y 10 m **sí hubo
registro**, y la etiqueta "nunca registrado en Colón" sería falsa en ese tramo.

### Riesgos conocidos de S1–S3

- **El presupuesto de 25 MB no está garantizado.** El peso de un GeoJSON crece con el área
  inundada, no con la cantidad de capas: a 13 m ya son 14.613 ha. Si se pasa, aplica S2
  (simplificar más y documentarlo), nunca recortar el rango.
- **Ampliar el BBOX al este cruza el río hacia Uruguay.** A 20 m es probable que el recorte toque
  borde igual, porque el río no cabe. `toca_borde` por capa deja eso registrado en vez de
  esconderlo.

### Herramientas

- **Playwright se autoriza como `devDependency`** para las capturas (pedido explícito del usuario).
  Es la única dependencia nueva de esta spec, y es sólo de desarrollo: no entra al bundle.
- Se mantiene **uPlot**; no se cambia de librería de gráficos.

## Archivos compartidos que puede tocar

- `backend/app/config/dominio.py` — **sólo** para agregar `CRECIDA_MAXIMA_OBSERVADA_M` con su
  fuente. No se modifica ningún valor existente.
- `frontend/public/capas/` — se regenera completa (S1).
- `geoprocessing/generar_capas.py` y su README.
- Todo `frontend/src/`.
- `frontend/package.json` — sólo para la devDependency de Playwright.
- No se toca `docker-compose.yml` ni `.github/workflows/`.

## Criterios de aceptación

- [x] L1: márgenes laterales ≤ 24 px; si hay tope de ancho, ≥ 1800 px. Medido en el navegador.
- [x] L2/L3: la grilla de cada breakpoint es la de la tabla, **sin celdas vacías**, y las tarjetas de
      una fila comparten altura. Verificado a 768, 1280 y 1920 px.
- [x] L4: ejes ≥ 13 px y contraste AA en los dos temas, medido (no a ojo). *(seguimiento T3bis:
      corregido — antes usaba `--fg` sin reactividad; ahora `--graf-eje` propio, releído en cada
      render, con ratio medido por `paleta.test.ts` en los dos temas, ver tabla abajo.)*
- [x] L5: la regla hidrométrica muestra los tres umbrales a escala y la altura de hoy, con texto.
      *(T3ter/G1: la marca estaba a escala pero la etiqueta de texto podía quedar corrida o fuera de
      la barra — corregido, ver T3ter.)*
- [x] M1: sin scroll horizontal a 360, 390 y 768 px; blancos de toque ≥ 44 px.
- [x] M2: mapa a 60vh en mobile con los controles debajo, sin taparlo.
- [x] M3: chips en scroll horizontal con indicador, ninguno cortado.
- [x] S1: 43 capas de 3,00 a 20,00 m con pasos 0,25 / 0,5 / 1,0. *(fuera de esta tarea: lo hace el
      agente de `geoprocessing/`/`capas/`, que trabaja en paralelo. Verificado en runtime que
      `nivel_max` ya llegó a 20,00 m en este worktree, pero no es responsabilidad de esta entrega.)*
- [ ] S2/S3/S4/S5: ídem, fuera de esta tarea.
- [ ] Escenario "9,06 máx. may 2024" renombrado, escenario "10 m" nuevo: ídem, fuera de esta tarea
      (vive en `capas.ts`/`map.ts`, no tocados acá).
- [x] T1/T2: toggle visible; respeta `prefers-color-scheme` por defecto; guarda en `localStorage`
      con `try/catch` y funciona si el almacenamiento falla.
- [x] T3: gráficos, leyendas, capas y controles legibles en ambos temas.
- [x] T3bis: paleta propia de gráficos con reactividad de verdad al tema (toggle y
      `prefers-color-scheme`), contraste WCAG medido por test (≥ 4,5:1 texto de ejes, ≥ 3:1 líneas
      de datos, los dos temas, ver tabla en "Hallazgos"), umbrales con patrón de guiones distinto
      entre sí, leyendas que coinciden con su línea, y mini gráfico en "Aguas arriba". Verificado
      con capturas por gráfico (390/1440 px × claro/oscuro) y con un script que confirma que el
      color de la línea "Altura real" cambia de verdad al tocar el toggle (no solo el CSS).
- [x] F: footer centrado con el texto y el link pedidos (`target="_blank"`, `rel="noopener"`).
- [x] Q: FAQ como acordeón antes del detalle técnico, con ancla `#faq` y link en el header; todas
      las respuestas con datos tomados del código, y las que falten marcadas PENDIENTE.
- [~] Capturas antes/después a 360, 390, 768, 1280 y 1920 px, en tema claro y oscuro. Se capturaron
      las de **"después"** (10 archivos, `/tmp/shots/despues2-*.png`, fuera del repo). Las de
      "antes" no se tomaron: había que evitar un `git stash` en un worktree donde otro agente tenía
      cambios propios sin commitear en paralelo (geoprocessing/capas). Ver "Hallazgos".
- [x] T3ter: los dos defectos graves (G1 regla corrida, G2 panel cerrado en mobile) y los menores de
      la revisión de diseño medida en navegador (G3-G5, blancos de "Superficie afectada" y FAQ, jerga)
      corregidos y medidos; ver sección T3ter y "Cómo verificar". Sigue pendiente, fuera de alcance de
      este worktree: el texto de API "Crecida de referencia" (backend) y una referencia comprensible
      para "26.094 ha" (no hay dato del que derivarla sin inventar un número).
- [ ] Revisión de diseño sin problemas graves abiertos. *(no corrida: no es una acción que este
      agente pueda ejecutar por sí mismo; queda para la revisión humana/RDD del PR. Esta entrega
      (T3ter) corrige los defectos que una revisión anterior ya había encontrado y medido.)*
- [x] `ruff`, `pytest`, `pnpm build` y `pnpm test` pasan.

## Cómo verificar

```bash
cd /Users/leandroalvarez/orca/workspaces/RioAltura/ui-v3
pnpm -C frontend exec tsc --noEmit
pnpm -C frontend build
pnpm -C frontend test
set -a; . ./.env.local; set +a; uv run ruff check . && uv run pytest -q
```

Con la web ya corriendo (`http://localhost:5204/`, API en 8031):

```bash
cd frontend
node scripts/capturas.mjs "http://localhost:5204/" /tmp/shots despues
```

**Paleta de gráficos (T3bis)**, con la web ya corriendo:

```bash
cd frontend
# Captura por-tarjeta (Evolución, Historial, Precisión, Aguas arriba) a 390/1440 px × claro/oscuro.
# Copia adaptada de capturas.mjs (no se toca ese archivo).
node scripts/capturas-graficos.mjs "http://localhost:5204/" capturas-008 graficos

# Confirma que el toggle redibuja de verdad (compara el color inline de la
# muestra de leyenda "Altura real" antes/después de tocar #theme-toggle).
node scripts/verificar-tema.mjs "http://localhost:5204/"
```

Y en el navegador (o con Playwright, ver el script usado para esta entrega):

- Ancho y márgenes a 1920: `document.querySelector('.dashboard').getBoundingClientRect()` — debe
  quedar centrada, ancho ≤ 1900 px, con ≤ 24 px de margen a cada lado.
- Sin celdas vacías: a 768/1280/1920, cada tarjeta de `.dashboard > *` tiene alto y ancho > 0
  (la única excepción esperada es `#tarjeta-que-hacer`, que sigue `hidden` por el flag apagado).
- Sin scroll horizontal: `document.body.scrollWidth <= window.innerWidth` a 360/390/768.
- Blancos de toque: `#theme-toggle`, `.topbar-link`, `.rango-selector button`, `.capas-escenario`,
  `.detalle-tecnico summary`, `.faq-pregunta`, `.capas-panel-resumen` miden ≥ 44 px de alto a 360 px.
- Ejes: `getComputedStyle(document.querySelector('.superficie-eje-etiqueta')).fontSize` → `13px`;
  los ejes de uPlot (`grafico.ts`/`historial.ts`/`precision.ts`) se configuraron con
  `font: "13px system-ui, sans-serif"` y `stroke` en `--fg` (alto contraste en los dos temas).
- Tema: click en `#theme-toggle`, recargar, y confirmar que `document.documentElement.dataset.theme`
  y `localStorage.getItem('rioaltura-tema')` mantienen la elección.

**T3ter (correcciones de revisión de diseño)**, con la web ya corriendo:

```bash
cd frontend
# G1 (desfase tick<->etiqueta de la regla + líneas guía), G2 (panel abierto),
# G3 (texto "Mostrando"), G4 (chevrones), G5 (marca de máximo observado) y
# blancos de "Superficie afectada", a 360/768/1280/1920 px.
node scripts/verificar-g1-g5.mjs

# Rótulos de umbral del gráfico con el número primero (no truncan el dato).
node scripts/verificar-grafico.mjs
```

Resultado real de esta corrida (`verificar-g1-g5.mjs`, tema claro, altura de hoy 4,29 m): a los
cuatro anchos, las 4 marcas de la regla (3 umbrales + hoy) quedaron `dentroDeLaBarra: true` y con
línea guía dibujada donde hubo desplazamiento (`cantidadGuias: 4` en los cuatro anchos); el panel del
mapa quedó `panelMapaAbierto: true` a 360/768/1280/1920; el texto del mapa fue "El mapa muestra la
altura más parecida que tenemos (4,25 m). Hoy el río está en 4,29 m."; sin scroll horizontal a
ninguno de los cuatro anchos. "Superficie afectada": alto de tarjeta vs. SVG de 328/120 (360),
294/120 (768), 702/494 (1280) y 702/528 (1920) — el hueco bajó de ~320/~270 px (medido antes del
arreglo) a ~208/~174 px en 1280/1920, y lo que queda es el título/subtítulo/slider/texto, no vacío.

## Hallazgos

### Tres defectos de la misma familia: geometría con números fijos

Los tres se veían bien mientras el contenedor era chico y se rompieron al
agrandarlo. **Ninguno lo detectó un test**: aparecieron mirando capturas y
midiendo el DOM. Es el punto ciego de este código.

1. **La etiqueta del eje de hectáreas.** Margen fijo de 58 px; al pasar el
   máximo a cinco dígitos, "26.094 ha" se dibujaba fuera del viewBox y se leía
   "6.094 ha" — el número principal de la tarjeta erraba por 20.000 ha. Ya había
   pasado antes, con 14.613 ha, y se había ajustado el margen a mano. Ahora se
   calcula desde la etiqueta más larga.
2. **La regla hidrométrica.** Dos intentos fallaron antes de dar con la causa.
   El problema no era el algoritmo anti-colisión sino la geometría: los tres
   umbrales están a 0,30 y 0,80 m entre sí sobre una escala de unos 4 m, así que
   **tres nombres de dos renglones no entran al lado de sus marcas**. Forzar un
   22 % de separación los mandaba media regla abajo, con "Alerta" al lado de la
   marca de 6,80 y "Evacuación" flotando fuera de la barra. Se resolvió mostrando
   sólo el número; los nombres ya viven en "¿Qué significa cada nivel?" y llegan
   a lectores de pantalla por `aria-label`.
3. **La curva de superficie.** Con `preserveAspectRatio="none"` y viewBox fijo,
   pasar la tarjeta a ancho completo la estiró 4,5× a lo ancho: el texto salía
   desparramado. Ahora el viewBox se arma con las medidas reales del contenedor.

**Lección para las próximas specs:** todo lo que calcule posiciones o tamaños en
píxeles tiene que derivarse del contenedor o del contenido, nunca de una
constante elegida para un tamaño de pantalla. Y hay que verificarlo mirando, no
sólo con tests: los tres pasaban con la suite en verde.

### Dos bugs que presentaban un pronóstico como una medición

Los dos aparecieron con datos reales, no con fixtures.

4. **El texto del mapa.** Arrastrar el slider a 17 m mostraba "El mapa muestra la
   altura más parecida que tenemos (17,00 m). Hoy el río está en 4,29 m." — 17 m
   no se parece a nada de hoy, es lo que el usuario pidió ver. El mensaje mezclaba
   el caso de redondeo con el de escenario elegido. Arriba de 13 m el caso es
   frecuente: las capas van de 1 en 1 pero el slider se mueve de a 0,25.
5. **La fila de hoy en "Próximos días".** Decía "3,93 m medido" cuando ese valor
   era la estimación pura de la curva. El código asumía que la altura anclada de
   hoy es ~igual a la real, y eso **sólo vale cuando el anclaje se aplicó**. Con
   el worker corriendo se comprobó que el caso es real y no teórico: el 22/09/2026
   ni el INA ni Prefectura tenían lectura de ese día, `anclaje.aplicado` vino en
   `false`, y sin el arreglo la app le habría dicho a un vecino que el río estaba
   *medido* en 3,93 m un día sin una sola lectura publicada.

### El panel del mapa: abajo no funciona

Se probó poner los controles debajo del mapa para ganar ancho, a pedido. **Se
pierde la idea**: nadie baja a buscar un slider que no ve, y con eso se pierde
"¿a qué altura se moja mi casa?", que es medio producto. Volvieron al costado
derecho. El ancho no se sacrificó, porque la curva de hectáreas se había mudado a
su propia fila: el lienzo mide 1.443 px a 1920, más que los ~1.200 que tenía
cuando compartía fila con ella.

En el panel lateral los nueve escenarios **envuelven** en varias filas en vez de
scrollear: el panel es angosto y abajo sobra alto, así que el scroll horizontal
escondía la mitad detrás de un gesto que nadie hace. En mobile el panel es ancho
y bajo, y ahí el scroll sigue siendo lo correcto (M3).

### La regla hidrométrica no venía de la 007

Ni "regla" ni "hidrométrica" aparecen en `specs/007-ui-v2.md`. Lo más parecido fue
una sugerencia de su revisión de diseño, que se resolvió por la otra vía que la
misma revisión proponía. Entró en esta spec como requisito nuevo.

### El contacto norte/sur del recorte

`toca_borde` da `true` en las 43 capas, pero es un agregado que incluye norte y
sur. El detalle por lado (`bordes`) muestra **este y oeste en `false` en todas**,
que es lo que pedía S3. El río cruza el recorte de norte a sur por definición, y
eso ya estaba documentado como advertencia desde la spec 006. No leer ese `true`
como una falla.

### Pendiente, fuera de alcance

- **Una respuesta del FAQ quedó marcada PENDIENTE**: "cómo reporto un error". No
  existe ningún canal de reporte en el código ni en `CLAUDE.md`, y no se inventó.
- **El retraso de las fuentes de altura.** El 22/09/2026, con el worker corriendo,
  el INA respondió 200 pero sin lecturas en 24 h y el respaldo cayó a Prefectura,
  que venía parada en el mismo instante. No es un problema de la UI. Se abre la
  spec 009 para sumar CARU como tercer eslabón: publica cada 12 h, tiene Colón
  (estación 12) y el 22/09 ya tenía el dato de hoy, con el mismo valor (4,29 m)
  que el INA — o sea, mismo cero del hidrómetro.
- **Los blancos dentro de tarjetas** se redujeron (Superficie afectada pasó de
  ~320 a ~208 px a 1280) pero no desaparecieron: es consecuencia del igualado de
  altura por fila que pide L3.

## Resumen final

Implementé L (grilla explícita con `grid-template-areas` por breakpoint, sin huecos, ancho ≤ 1900 px),
M (mobile sin scroll horizontal, toques ≥ 44 px, chips con scroll + degradé), T (toggle de tema con
`try/catch`, tokens CSS en los dos temas), F (footer centrado con la línea de Mirai Software) y Q
(FAQ-acordeón con las 11 preguntas, datos leídos del código/CLAUDE.md, 1 PENDIENTE real: canal de
reporte de errores). `tsc`, `pnpm build`, `pnpm test` (160/160) y `ruff`/`pytest` (180/180) pasan.
La única duda real es el agrupamiento visual del panel lateral del mapa (ver Hallazgos): lo resolví
con CSS puro para no tocar `map.ts` de la spec 006.

### Resumen — seguimiento T3bis (paleta reactiva de gráficos)

Fuente única `frontend/src/graficos/paleta.ts` (colores por rol/tema, anchos/guiones, contraste
WCAG, CSS generado); `theme.ts` expone `onTemaCambia`, los cuatro gráficos se suscriben y redibujan
sin refetch, y devuelven `{ destroy() }` para desuscribirse. Ningún color de la tabla pedida
necesitó ajuste (todos pasan el contraste mínimo, ver Hallazgos). Mini gráfico nuevo en "Aguas
arriba" con función pura testeada. `tsc`, `pnpm build` y `pnpm test` (188/188, incluye 20 tests de
contraste + 3 de `onTemaCambia` + 2 de la serie del mini gráfico) pasan; `ruff`/`pytest` sin cambios
(180 passed, 4 skipped) porque no se tocó backend. Redibujo real confirmado en navegador (toggle y
`prefers-color-scheme`), no solo cambio de CSS.

### Resumen — T3ter (defectos de la revisión de diseño)

Corregí los dos defectos graves (G1: algoritmo de dos pasadas + línea guía en la regla hidrométrica,
para que ninguna etiqueta quede fuera de la barra ni sin marca visual de su tick; G2: panel del mapa
abierto por defecto en cualquier ancho) y los menores (G3 texto "Mostrando" explicado, G4 chevrones
en los tres `<summary>`, G5 marca de "máximo observado" en la curva de superficie, blancos de
"Superficie afectada"/FAQ, jerga). `describeMostrando` y `xParaAltura` son funciones puras nuevas,
testeadas. `tsc`, `pnpm build` y `pnpm test` (212/212, incluye 15 tests nuevos: 6 de la regla, 6 de
`describeMostrando`, 3 de `xParaAltura`) pasan; `ruff`/`pytest` sin cambios (180 passed, 4 skipped).
Verificado en navegador con dos scripts nuevos (`verificar-g1-g5.mjs`, `verificar-grafico.mjs`), ver
resultados reales en "Cómo verificar". No toqué el texto de API "Crecida de referencia" (backend, no
`ui-v3`) ni inventé una referencia para "26.094 ha" sin dato del que derivarla.
